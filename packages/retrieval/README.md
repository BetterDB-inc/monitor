# @betterdb/retrieval

Developer-facing retrieval SDK over valkey-search: index lifecycle, upsert, vector + filtered query.

> **Status:** API is under construction — Phase 1 of the retrieval SDK plan. No public surface is stable yet.
