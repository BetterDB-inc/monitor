"""LongMemEval retrieval harness for betterdb-retrieval (Python port).

Python equivalent of ``packages/retrieval/eval/longmemeval`` (TypeScript). Same
seam architecture (Embedder / Store / Reader / Judge), same tiers, same bundled
fixture, run with ``python -m eval.longmemeval`` from ``packages/retrieval-py``.
"""
